# 배포된 백엔드를 직접 호출할 때 사용. 비워두면 vite dev 프록시(/api)를 탄다.
VITE_API_BASE_URL=

# vite dev 프록시가 바라볼 로컬/개발 백엔드 주소
VITE_DEV_API_TARGET=http://localhost:8080

# 백엔드 없이 화면만 테스트할 때 true 로 두면 MSW 목 서버가 뜬다.
VITE_USE_MOCK=true
